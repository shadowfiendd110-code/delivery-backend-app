services:
  backend:
    image: delivery-backend
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development

  frontend:
    image: delivery-frontend
    ports:
      - "8081:80"
    depends_on:
      - backend